{
  "name": "websim-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for WebSim API - Access project data, user profiles, and trending content",
  "main": "index.js",
  "module": "./src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/start.js",
    "start:dev": "ts-node src/start.ts",
    "test": "echo \"No tests specified\"",
    "test:unit": "echo \"No unit tests specified\"",
    "test:integration": "echo \"No integration tests specified\"",
    "lint": "eslint src --ext .ts",
    "lint:fix": "eslint src --ext .ts --fix",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean"
  },
  "keywords": [
    "mcp",
    "smithery",
    "websim",
    "web-simulation",
    "projects",
    "api"
  ],
  "author": "MiniMax Agent",
  "license": "MIT",
  "dependencies": {
    "@smithery/sdk": "^1.4.5",
    "@modelcontextprotocol/sdk": "^1.18.1",
    "zod": "^3.25.46",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@smithery/cli": "^1.4.5",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "eslint": "^8.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}