# WebSim MCP Server Environment Variables
# This server uses the public WebSim API which doesn't require authentication
# Leave these values empty or unset for public access

# Optional: Custom User-Agent for API requests
# WEBSIM_USER_AGENT=websim-mcp-server/1.0.0 (your-contact-info@example.com)

# Optional: API request timeout in milliseconds
# WEBSIM_TIMEOUT=30000

# Optional: Rate limiting settings
# WEBSIM_RATE_LIMIT_REQUESTS=100
# WEBSIM_RATE_LIMIT_WINDOW=60000

# Note: WebSim API is currently public and read-only
# No authentication or API keys are required